# Authoritative multiplayer command coverage

This is the completion ledger for API-v3 gameplay authority. It is based on
current source call sites, not intended behavior. A row is **Complete** only
when the public command is closed and versioned, the Kotlin worker calls shared
domain logic, Rust derives the actor from session and membership, legality and
authorization tests pass, the player projection contains the data needed to
issue and reconcile the command, and every online UI call site uses the v3
command bus. A server handler without migrated client call sites is **Partial**.

## Current authority boundary

Explicit legacy/API-v2 online play remains client-authored:
`WorldScreen.nextTurn()` clones and advances `GameInfo`, then calls
`Multiplayer.updateGame()`; `Multiplayer.kt` also mutates resignation/turn
state locally; and `MultiplayerServer.uploadGame()` sends the resulting whole
save. Current API-v3 creation, discovery, world, end-turn, and resignation
routes never enter those paths. Historical legacy-shaped v3 interceptors are
still being removed or classified by the source-level boundary audit.

API v3 now exposes a closed command union spanning movement, combat, unit
orders/actions, city production/economy/population/governance, research,
policies, religion, trades, diplomacy, city-state interactions, espionage, voting,
great-person choices, and end turn. The production `Multiplayer` owner installs one
`AuthoritativeMultiplayerSession`, which negotiates capabilities, restores or
creates an authenticated session, opens per-game command buses from an HTTP
projection, reconciles WebSocket hints through HTTP, and pages through the
authenticated account's server-owned game memberships. For an explicitly
opened v3 game, `WorldScreen.nextTurn()` submits through that lifecycle before
cloning or advancing local state and never uploads the result. Rows below remain
partial or missing where current call sites or required projection data are not
yet migrated. API-v3 game creation, discovery, and projection reads are
implemented outside the gameplay command union.

Status meanings:

- **Partial**: some authoritative protocol/worker/server/test support exists,
  but required commands, projection data, or client migration remains.
- **Missing**: no v3 gameplay command and no authoritative handler exist.
- **Not applicable**: local-only presentation behavior that must remain local.

## Coverage matrix

| Mutation family | Required typed command(s) | Shared handler / current mutator | Server authorization and validation required | Projection and reconciliation effects | Current online call sites / evidence | Status |
|---|---|---|---|---|---|---|
| Server game creation | `CreateGame` resource creation (never a state-upload command); the backend accepts a caller-stable operation ID, pinned manifest hash, and one closed bounded generated-game setup, then generates an independent OS-backed secret map seed inside the control plane | `GameStarter.startNewGame` through `HeadlessGameEngine.createGame`; the private worker independently validates ruleset-derived choices and materializes `GameSetupInfo` from the typed setup, pinned manifest, authenticated owner, and server seed | Authenticated owner; durable operation identity is bound to the account and canonical manifest/setup meaning; concurrent duplicates serialize and return one game, while changed reuse fails closed; server-approved engine/ruleset manifest; bounded fixed civilization counts, map enum choices, toggles and timers; difficulty/speed/era/victories must exist in the pinned ruleset; no client seed, clock, arbitrary/custom map, setup JSON, `GameInfo`, snapshot, actor, or revision-zero result; the seed is not derivable from the public game UUID | Initial player projection, membership, revision `0`, and creation-operation record commit atomically; failed creation leaves neither; the authenticated session requires the operation ID, resolves one exact manifest, creates, fetches the projection, and opens the command bus | Production `NewGameScreen` routes authenticated v3 setup through the server, preserves one operation ID for unchanged meaning, rejects unsupported custom/client-authored choices, validates owner membership/revision/hash/civilization against the returned projection, and opens the projection-only world. Explicit legacy servers cannot create new online games | **Complete for bounded generated-game production creation and projection-world entry** |
| Join and civilization assignment | Owner-created durable player invitation plus closed `JoinGame` | `HeadlessGameEngine.assignPlayer` | Enabled target account discovers only its own pending invitations; invitation is required before worker execution and rechecked under the final game lock; server selects an unclaimed civilization; one account/civilization; join uses the actual current revision rather than a revision-zero special case | Invitation consumption, membership/civilization, canonical snapshot, revision, journal, and outbox commit atomically; subsequent invited players can join later revisions | The production account inbox uses retry-stable acceptance, rediscovers the committed membership, validates the server-assigned civilization and revision/hash, opens the player projection, and transitions directly into the projection-only world. No client civilization or player ID is submitted | **Complete** |
| Unit movement | `MoveUnit(unitId, destination, escortUnitId?)` implements exact current-turn single or atomic escort-pair movement, `MoveUnitToward(unitId, finalDestination, escortUnitId?)` implements canonical single or paired multi-turn orders, `CancelUnitMovementOrder(unitId)` cancels an existing canonical route through either pair member, and `SwapUnits(unitId, destination)` implements compatible friendly occupancy | Shared `UnitMovement.moveToTile`, transient in-worker escort reconstruction, `headTowards`, serialized `MapUnit.action` plus stable nullable `movementEscortUnitId` processing/clearing, and `swapMoveToTile(..., keepEscorting = true)` through focused movement modules and `HeadlessGameEngine` handlers | Membership-derived current actor/turn; pair preflight requires projected owned units to be co-located. The worker revalidates distinct IDs, ownership, co-location, one civilian plus one military unit, non-air types, bounds, reachability, entry legality, movement, terrain, and occupancy before moving both in one proposal; no client path, cost, compatibility result, or outcome is trusted | Projection v58 retains owner-only movement routes and their optional companion ID plus sorted, distinct exact-move, multi-turn, and swap destinations for the authenticated current actor. Paired exact destinations are the canonical intersection for both units. Foreign order metadata and all foreign/out-of-turn action choices are absent | The production projection-only world and opened-v3 controls consume cached exact destinations and fail closed when unavailable. Exact and multi-turn moves send only stable unit IDs and a destination; snapshot reload reconstructs transient shared-engine escort state only inside the worker. Local, hotseat, and legacy behavior is unchanged | **Complete for inventoried movement, durable escort routes, cancellation, and swaps** |
| Unit melee/ranged/city combat | `AttackWithUnit(unitId, targetX, targetY)`, `BombardWithCity(cityId, targetX, targetY)`, `LaunchNuclearStrike(unitId, targetX, targetY)`, and `AirSweep(unitId, targetX, targetY)` | Focused Kotlin executors delegate to existing `Battle`, `BattleDamage`, `Nuke`, `TargetHelper`, and `AirInterception` engines | Membership-derived actor/current turn; canonical ownership, capability, attacks/movement, setup, visibility/exploration, range/path/line of sight, war state, target/interceptor selection, blast victims, and deterministic RNG; clients claim no defender, path, strength, modifier, damage, victims, random values, or outcomes | Projection v45 publishes sorted bounded visible attack/attack-from and bombard targets with canonical base/effective strengths, modifier summaries, health ranges, and closed capture outcomes. Nuclear candidates add only the own weapon's blast radius and a closed hidden-until-commit disclosure; air-sweep candidates add only own attacker strength/modifiers/health and the same closed interceptor disclosure. Candidate membership and metadata are invariant when hidden blast victims or interceptors change; accepted commands revalidate and reconcile every canonical result | Opened-v3 right-click, highlighting, preflight, confirmation, and submission consume only cached projected choices and previews. The confirmation panel performs no local combat calculation and explicitly explains delayed nuclear/interceptor disclosure. Local, hotseat, legacy multiplayer, and server AI retain shared engine paths | **Complete** |
| Unit special actions | `DisbandUnit`, `FoundCity`, `ParadropUnit`, `UseReligiousUnit`, closed `UseGreatPersonUnit`, `GiftUnit`, `AddUnitToCapitalProject`, opaque `CreateInstantImprovement`, `TransformUnit`, and `TriggerUnitUnique` are implemented; the airlift audit found rebasing already uses authoritative movement | Existing shared unit actions and `UnitActionsFromUniques` callbacks through focused authoritative executors; Rust contains no rules engine | Membership-derived current actor/turn and owned unit; projection-bound closed or opaque action identity; the worker re-derives targets, tiles, improvements, costs, resources, movement, conditionals, RNG, consumption, placement, capital/project identity, and effects | Projection v57 exposes exact disband/pillage/found capability and sorted visible paradrop destinations alongside applicable religious, great-person, gift, capital-project, instant-improvement, transform, and trigger actions only for owned current-turn units; canonical effects reconcile after commit | Both legacy-shaped opened-v3 controls and the disposable projection-only world submit through the authenticated session before local mutation. The projection-only controller rejects invented capability and coordinates before transport; local, hotseat, saves, legacy multiplayer, and server-owned AI retain shared Kotlin behavior | **Complete for the inventoried direct unit special-action surface** |
| Unit automation, orders, and postures | `SetUnitExploration(unitId, enabled)` and `SetUnitAutomation(unitId, enabled)` implement exploration and generic automation start/stop; closed-enum `SetUnitPosture(unitId, posture)` implements Sleep, Sleep Until Healed, Fortify, Fortify Until Healed, Guard, and siege Setup; `SetTileImprovementOrder` handles same-tile worker construction/cancellation/repair; nullable-destination `SetRoadConnectionOrder` starts and cancels road automation | Shared `UnitAutomation`, `RoadToAutomation`, posture/setup methods, tile improvement queues, durable paired movement orders, and existing canonical action semantics through focused modules and `HeadlessGameEngine`; pending automated unit actions execute in the private worker immediately before authoritative end-turn processing | Membership-derived current actor/turn; own-unit preflight; canonical ownership, movement, escort compatibility, siege unique/embarkation/setup state, builder uniques, road technology, explored/passable/buildable destination, server-derived path, healing, fortification, improvement legality, repair state, and Guard validation; arbitrary action strings and client paths are impossible | Projection v58 retains owner-only automation/exploration/order state and advertises exact worker-derived posture, improvement/repair/cancellation, and road-destination choices. Visible foreign and out-of-turn units receive false/empty/null private controls; Rust rejects reordered, duplicate, malformed, foreign, and out-of-turn choices | The projection-only world renders exact exploration/automation flips, movement cancellation, promotions, swaps, postures, multi-turn destinations, improvement orders, and road start/cancellation. API v3 deliberately does not expose legacy whole-turn autoplay because that implementation runs client AI through `WorldScreen`; all real AI and pending unit automation run in the private worker. Local, hotseat, and legacy retain their existing autoplay controls | **Complete for the retained authoritative order surface** |
| Promotions, upgrades, and unit names | Bounded `UpgradeUnits`; bounded `PromoteUnit` with server-derived optional city-default capture; `SetCityUnitPromotionPreference(cityId, baseUnitName, enabled)`; nullable `RenameUnit` | Shared upgrade, `UnitPromotions.addPromotion`, city saved-promotion application, and `MapUnit.instanceName` | Membership-derived current actor/turn and stable owned IDs. Upgrade and promotion legality is canonical. Saving defaults requires the promoted unit to occupy an owned non-puppet city and clones its canonical promotion state. Preference toggles require an existing server-saved entry and accept no promotion/XP payload. Rename accepts null or 1-100 printable characters | Projection v57 adds sorted, affordable, exact per-unit upgrade targets with canonical gold cost plus turn-scoped rename capability to the existing owner-only promotion/XP/name data. Foreign and out-of-turn projections expose none of these action choices | The disposable projection-only world now renders promotions, each exact upgrade target/cost, and bounded rename input through the same authenticated command bus. Legacy-shaped opened-v3 Upgrade All, saved-default, and rename callers remain authoritative; local, hotseat, saved-game, legacy multiplayer, and server-internal paths remain local | **Complete for discovered normal promotion, upgrade, saved-default, and rename UI paths** |
| Worker improvements and pillage/repair | Closed nullable `SetTileImprovementOrder(unitId, improvementName, queuedImprovementName)` implements start/replace/cancel and Repair; nullable-destination `SetRoadConnectionOrder(unitId, x, y)` implements road start/cancel; `PillageTile(unitId)` contains no tile, target, loot, healing, cost, RNG, or actor claims | Shared tile improvement methods, `RoadToAutomation`, focused `UnitOrderProjection`, and domain `UnitPillage` rules through `HeadlessGameEngine`; human UI, AI automation, battle auto-pillage, and the private worker use the same canonical implementation | Membership-derived current actor/turn and owned unit; server derives current tile, improvement-before-road target priority, legality, fixed/random loot, closest recipient city, health recovery, movement cost, resource changes, notifications, destroyed-on-pillage behavior, and road path; submitted choices must match the current exact projection and are independently revalidated | Projection v58 exposes bounded exact improvement, queued follow-up, cancellation, and reachable road-destination choices only for current-turn owned units, while current order state remains owner-only. Rust validates bounds, ordering, uniqueness, cancellation coherence, explored coordinates, and absence from foreign/out-of-turn units | The production projection-only world and opened-v3 controls render exact worker choices and submit typed intents without local canonical mutation. Local/hotseat/legacy paths retain shared behavior | **Complete for construction, repair, road orders, and direct pillage; broader worker automation coverage remains tracked separately** |
| City production queue | `QueueConstruction`, `QueueConstructionAtTile`, `RemoveConstruction`, `MoveConstruction`, `SetPerpetualConstruction`, and closed-enum `ManageConstructionQueues` implement every inventoried queue operation: move to top/end, add to top, add to all cities, add-or-move to top in all cities, remove from all cities, and enable/disable construction automation in one or every city | Shared `CityConstructions` and canonical city/civilization disabled-construction state through `HeadlessGameEngine`; the worker derives the bounded target-city set and applies an all-or-reject canonical proposal. Tile-targeted buildings have a distinct coordinate-bearing command and perpetual choices use a distinct replacement command | Membership-derived civilization/current turn, anchor-city ownership, exact optional queue index/name, canonical candidate cities, puppet/resistance/razed exclusions, constructability/capacity, wonder and tile-target restrictions, idle-construction exclusion, and at least one real mutation. Clients cannot send target cities, actor, legality, partial results, or outcomes | Projection v60 retains the ordered queue, cost/turn metadata, exact queueable choices, placement targets, and sorted closed action allowlists, including the four canonical automation actions. It also publishes durable world-wonder completion events with ruleset-derived effect summaries, knowledge-gated builder identity, and exploration-gated city/location fields, plus the public canonical victory result | Production queue controls submit all context actions only when advertised by the cached projection. The projection-only history panel renders exact public wonder events without reconstructing hidden builder or location context. Accepted, stale, and retry behavior reconciles through the command bus/session; local, hotseat, and legacy behavior remains local | **Complete for inventoried queue controls and public wonder completion/effect projection** |
| City purchases | `PurchaseConstruction(cityId, constructionName, currencyName, queueIndex?)` and `PurchaseConstructionAtTile(..., x, y, queueIndex?)` implemented | Shared `CityConstructions.purchaseConstruction` through the corresponding headless-engine handlers | Membership-derived civilization, current turn, city ownership, bounded canonical name/index/coordinate, supported currency, construction and placement eligibility, resources and server-recomputed cost; actor, price, and legality claims are absent from both wire contracts | Projection v46 publishes each supported currency's canonical cost, actor reserve, allowed flag, tile requirement, and exact legal targets for available and queued constructions | `BuyButtonFactory.kt`, tile picking, confirmation balances, and command-bus preflight consume only cached projected purchase metadata for opened-v3 games; accepted commands reconcile canonical treasury/resources/queue/results. Local, hotseat and legacy games retain shared local behavior | **Complete for inventoried single-construction purchases** |
| Citizen and specialist assignment | `SetCityTileAssignment`, absolute `SetSpecialistCount`, boolean `SetManualSpecialists`, zero-option `ResetCitizens(cityId)`, boolean `SetAvoidGrowth`, and closed-enum `SetCitizenFocus` implemented | Shared city worked/locked collections, specialist allocations, `setAvoidGrowth`, `setCitizenFocus`, `reassignPopulation()`, and `reassignPopulation(resetLocked = true)` through focused `HeadlessGameEngine` handlers | Membership-derived civilization, current turn, city ownership, non-puppet/non-resistance state, canonical tile legality or specialist identity/capacity, canonical free population, player-selectable focus, and religion compatibility; client population, capacity, yields, and legality claims are absent | Projection v8 exposes each owned city's allowlisted tiles, specialist current/capacity pairs, manual-specialist state, avoid-growth state, current focus, and selectable focus allowlist | All existing opened-v3 citizen-management controls route through the session/command bus without local mutation. The worker performs canonical reassignment after each accepted change; local, hotseat, and legacy behavior is unchanged | **Complete for inventoried citizen-management controls** |
| City tile acquisition and tile-specific construction | `BuyCityTile`, bounded `BuyCityTileBatch(cityId, ring)`, `QueueConstructionAtTile`, and `PurchaseConstructionAtTile` implemented | Shared `CityExpansionManager.buyTile`, focused deterministic `CityTileBatchPurchaseExecutor`, `CityConstructions.addToQueue(..., tile)`, and `purchaseConstruction(..., tile)` through `HeadlessGameEngine`; batch order and escalating costs are derived only by the Kotlin worker | Membership-derived civilization, current turn, city ownership, canonical map coordinate or bounded ring, canonical placement/ownership/range/contiguity rules, constructability, treasury and server-recomputed prices; a batch is fully affordable before any mutation, and wire contracts contain no actor, tile list, price, result, or client legality claim | Projection v51 retains single-tile ownership/work/purchase metadata and adds only complete deterministic batch options with ring, tile count, canonical sequential cost, and affordability | `CityScreen` labels/highlights tiles and confirms/preflights single-tile acquisition, authoritative context-menu batches, and tile-targeted construction only from cached projected metadata before routing through v3. Accepted and lost-response retries reconcile canonical results; local/hotseat/legacy behavior is unchanged | **Complete for inventoried tile acquisition and tile-specific construction interactions** |
| Captured-city decisions | `ResolveCityDisposition(cityId, action)` with closed `liberate`, `annex`, `puppet`, `raze`, and `destroy`; ordinary owner governance uses `SetCityGovernance` for annex/start-raze/stop-raze | Focused `CityDispositionExecutor` and `CityGovernanceExecutor` delegate to existing city conquest/governance methods | Membership-derived current actor; exact pending alert; city ownership/founder; annex restriction; one-city challenge; capital/holy-city destruction constraints; selected action must be server-projected; one-shot consumption | Projection exposes only the actor's pending city IDs, names, and legal action allowlists plus owned-city governance state; ownership, city removal/status, diplomacy, and notifications reconcile after commit | Opened-v3 conquest popups and city governance controls submit through the session before local mutation and close only after server reconciliation. Local, hotseat, legacy multiplayer, and server AI retain shared engine behavior | **Complete for inventoried capture disposition and owner-governance controls** |
| Research selection, queue control, and completion | `SetResearchPath` supports replace and append intent, `ManageResearchQueue` supports closed move-to-top/up/down/end and remove actions, `ChooseFreeTechnology` is implemented, and `AcknowledgeResearchCompletion(promptId)` consumes an opaque projected completion | `TechManager.getRequiredTechsToDestination`, focused `ResearchQueueOperations`, canonical progress/cost and numeric turn-estimate calculation, `getFreeTechnology`, and canonical `TechResearched` alert consumption through `HeadlessGameEngine`; the server derives every prerequisite-safe queue proposal and never accepts a client-authored queue or completion result | Membership-derived actor/civilization and current turn; exact technology and queue index; action must be advertised for that entry; bounded technology in the pinned ruleset; prerequisite topology; projection-scoped replace/append/free target; opaque 64-hex completion identity; the server derives paths, queue results, free-tech legality, and the exact pending alert | Projection v49 includes researched history, queue metadata and per-entry closed action allowlists, legal targets, and only the actor's pending public research completions as opaque IDs plus technology names; unrelated/private alerts are excluded | `TechPickerScreen.kt` renders only projected queue context actions and submits through the opened authoritative session; it remains projection-driven for selection, while `AlertPopup.kt` acknowledges completion before removing its disposable local presentation. Accepted commands reconcile server state. Local, hotseat, saves, legacy multiplayer, and server-owned AI retain shared Kotlin behavior | **Complete for inventoried research controls** |
| Policies and ideology | `AdoptPolicy` covers ordinary policies, branch openers, ideology selection, subsequent tenets, and arbitrary mod-defined policy choices through one closed name-only intent | `PolicyManager.canAdoptPolicy`, `isAdoptable`, and `adopt` through `HeadlessGameEngine.adoptPolicy`; no ideology names or policy rules are duplicated in Rust | Membership-derived actor/civilization and current turn; exact choice must be in the actor projection; bounded policy in pinned ruleset; canonical culture/free-policy balance, era, prerequisites, mutual-exclusion availability conditions, triggers, and automatic branch completion | Projection v50 retains the actor's culture, costs, adopted names and exact selectable set, and adds each known major civilization's sorted public adopted policy-branch names for ideology/global-politics presentation; Rust bounds and validates ordering, uniqueness, and names | `PolicyPickerScreen.kt` routes every policy and branch confirmation for an explicitly opened v3 game through the session/command bus without local adoption. Accepted commands reconcile canonical state; local, hotseat, saves, legacy multiplayer, and server-owned AI retain shared engine behavior | **Complete for inventoried policy and ideology controls** |
| Pantheon, religion, enhancement, beliefs | `UseReligiousUnit` and `ChooseReligiousBeliefs` cover founding/expanding pantheons, founding/enhancing religion, free-belief reform, spread religion, remove heresy, and trigger uniques | Existing religion managers plus focused `ReligiousActionExecutor` and `ReligionChoiceExecutor` in the private Kotlin worker | Membership-derived actor/current turn; owned unit and projected action; canonical faith, charges, founding city, pending choice mode, exact belief slots, global availability, icon/name rules, and conditional availability | Projection v23 exposes only the actor's pending belief-slot choice, eligible beliefs/icons, identity requirement, and owned-unit action allowlists; canonical religion, holy city, pressure, faith, and triggers reconcile after commit | Explicitly opened v3 religious unit actions and both belief pickers submit through the session before legacy callbacks. Local, hotseat, saves, legacy multiplayer, and server AI retain shared Kotlin behavior | **Complete for inventoried player religious actions; rename-only UI remains unsupported unless separately inventoried** |
| Major-civilization diplomacy and war | `DeclareWar`, `DenounceCivilization`, `OfferFriendship`, `MakeDiplomaticDemand`, and `RespondToDiplomaticPrompt`; peace remains an authoritative bilateral trade | Existing `DiplomacyManager`, `Demand`, and focused `DiplomacyCommandExecutor` in the private Kotlin worker | Membership-derived actor/current turn; known living major counterpart; canonical relationship-changing rules, peace-treaty cooldown, diplomatic status, pending prompt identity/type/requester, and demand availability are re-derived; clients cannot claim modifiers, duration, war reason, response outcome, or actor | Projection v25 exposes eligible partners and action allowlists plus only the authenticated civilization's actionable friendship/demand prompts with opaque stable IDs; canonical war, denunciation, friendship, promises, alerts, and diplomacy state reconcile after commit | Explicitly opened v3 major-civilization diplomacy screens and actionable friendship, demand, and denouncement alert choices submit through the authoritative session before every legacy mutation. Local, hotseat, saves, legacy multiplayer, and server-owned AI retain shared Kotlin behavior | **Complete for inventoried direct major-civilization actions; city-state protection/war prompts remain in the city-state row** |
| Trades and agreements | `OfferTrade`, `RetractTradeOffer`, `AcceptTrade`, `DeclineTrade`, and atomic `CounterTrade` with stable server-derived request IDs | Shared `TradeLogic`, `TradeRequest.decline`, and focused `TradeCommandExecutor` in the private Kotlin worker | Membership-derived actor/current turn; known major partner; pending request ownership; exact canonical offer availability and amount; treaty/resource/city/war rules are re-derived; no client-supplied transfer outcome, evaluation, actor, or whole save | Projection v24 exposes player-scoped available bilateral offers, outgoing-pending state, and only the actor's incoming requests with opaque IDs; canonical treasury/resources/cities/treaties and both sides' request views reconcile after commit | Explicitly opened v3 trade table and popup route offer, withdrawal, acceptance, rejection, and atomic counteroffer through the command bus before legacy mutation. Local, hotseat, saves, legacy multiplayer, and server AI retain shared Kotlin behavior | **Complete for inventoried bilateral major-civilization trade UI; city-state interactions remain separate** |
| City-state interactions | `GiftCityStateGold`, `SetCityStateProtection`, `DemandCityStateTribute`, `GiftCityStateImprovement`, `DeclareWar`, `NegotiateCityStatePeace`, `MarryCityState`, and `RespondToCityStateProtectionPrompt` | Existing `CityStateFunctions`, `TradeLogic`, and diplomacy manager through focused `CityStateCommandExecutor` and `DiplomacyCommandExecutor` in the private Kotlin worker | Membership-derived actor/current turn; known living counterparts; war/peace and ally-war state; treaty and marriage cooldowns; canonical gifts, improvements, pledge/tribute rules, marriage unique/alliance/cost/transfers, pending prompt identity/type, relationship mutability, special war reason, influence, condemnation, and pledge withdrawal are all re-derived | Projection v29 exposes each known city-state's exact legal direct actions and only the authenticated civilization's actionable bullied/attacked protector prompts through opaque IDs and typed response allowlists, without military calculations, influence internals, hidden tiles, RNG, or outcomes | Explicitly opened v3 city-state screens and protector/ally alert choices submit through the session before every legacy mutation. Local, hotseat, saves, legacy multiplayer, and server AI retain shared Kotlin behavior | **Complete for inventoried direct city-state and protector-response actions** |
| Espionage | `MoveSpy(spyName, cityId?)` and `SetSpyCoup(spyName, enabled)` | Existing `Spy.moveTo` and `Spy.setAction` through focused `EspionageCommandExecutor`; tech theft, election rigging, coup resolution, counterintelligence, progression, and RNG continue in server-owned `Spy.endTurn` | Membership-derived current actor/turn; bounded owned spy identity; living state; explored, unoccupied destination; current assignment; canonical coup eligibility and cancellation; clients cannot submit target civilization, timing, probability, influence, RNG, or outcomes | Projection v30 exposes only the authenticated civilization's spy identities, ranks, assignments, closed actions, remaining turns, legal explored destination IDs, and coup/move allowlists; other civilizations' spies and private results remain absent | Every move-to-city, move-to-hideout, stage-coup, and cancel-coup control in `EspionageOverviewScreen.kt` returns after v3 submission before local mutation. Local, hotseat, saves, legacy multiplayer, and server-owned AI retain shared Kotlin behavior | **Complete for inventoried player-authored espionage controls** |
| Diplomatic/world-congress votes | `CastDiplomaticVote`; future congress proposal/vote variants remain separate | Existing diplomatic-victory vote state through the private Kotlin worker | Membership-derived actor/current turn; pending ballot; projected living-major candidate or abstention; one canonical ballot | Projection exposes only legal candidate IDs and the actor's pending-vote blocker; other civilizations' unrevealed votes remain absent | Explicitly opened v3 diplomatic vote picker submits through the authoritative session before local mutation; local, hotseat, saves, legacy multiplayer, and server AI retain shared Kotlin behavior | **Complete for the current diplomatic-victory ballot; future congress mechanics not present in this flow remain separate** |
| Golden age and other civilization choices | `ChooseGreatPerson` and `ResolveEventChoice(promptId, choiceId)` | Existing great-person manager and `EventChoice.triggerChoice` through focused private-worker executors | Membership-derived current actor/turn; canonical pending grant or event alert; unit context; current event/choice conditions; opaque server-issued IDs; one-shot resolution; all nested effects and RNG execute in the worker | Projection v32 exposes selectable great people plus only the authenticated civilization's pending event names, display text, owned-unit context, and opaque available choices; no uniques, conditions, RNG, or outcomes | Great-person picker and `AlertPopup` event buttons submit through v3 before local mutation. Local, hotseat, saves, legacy multiplayer, tutorials, and server-owned AI retain shared Kotlin behavior | **Complete for inventoried civilization-level choice prompts; unit actions are tracked separately** |
| End turn | `EndTurn` | Worker-owned pending automated-order phase followed by `GameInfo.nextTurn(executionContext)` through `HeadlessGameEngine.endTurn` | Current actor from membership and current civilization; the worker re-derives and rejects unresolved construction, technology/free-tech, policy/ideology, pantheon, religion founding/enhancement/reform, diplomatic-vote, or great-person choices without mutation; transient idle-unit and move-spies reminders are deliberately non-canonical; server clock/RNG | New revision, turn metadata, automated orders, all AI/rotation effects, next-player notification, and refreshed projection | `AuthoritativeWorldScreen` derives synchronization/waiting and mandatory-action navigation from the immutable projection and submits each typed resolution before `EndTurn`; it never advances locally. The historical `WorldScreen` API-v3 interceptor is removed, leaving that screen explicitly local/legacy. Rust validates that the blocker list is closed, ordered, unique, and backed by corresponding projected choices | **Complete for authoritative turn readiness, advancement, and production routing** |
| Resign and force-resign | Closed zero-payload `Resign` and `ForceResign` | Shared `GameInfo.nextTurn(executionContext)` and notification behavior through focused lifecycle methods in `HeadlessGameEngine` | Self target derives only from authenticated membership. Force-resign requires the authenticated owner role; Kotlin selects only the canonical current human player and compares the server clock with canonical `currentTurnStartTime` plus canonical `playerMinutesBeforeForceResign`. PostgreSQL removes the worker-identified membership in the same transaction as snapshot/revision/journal/outbox | Target civilization becomes AI, player identity is cleared, current-turn AI/rotation runs server-side, and a public notification is committed. Self resignation terminates the former player's projection; force-resign reconciles the owner's projection | Explicitly opened v3 self/force controls submit typed commands and never download/upload a save. Local, hotseat, saves, and legacy multiplayer retain existing behavior | **Complete for inventoried resignation lifecycle** |
| Spectator join/visibility | Owner-only `PUT /spectators` invitation, owner-only retry-safe `POST /spectator-revocations`, self-only `DELETE /spectators`, and a distinct read-only spectator projection | `SpectatorProjectionBuilder` derives an explicit public summary from the canonical snapshot through the private Kotlin worker; Rust owns membership, revocation, and read authorization without fabricating gameplay revisions | Authenticated owner adds an enabled account; target cannot already hold another role; add is idempotent; owner revocation is durably bound to one operation ID, actor, normalized target, and meaning; spectators have no civilization and cannot use the player projection; only the spectator can self-leave | Projection v1 contains only turn, current civilization ID, and sorted major-civilization public identity/control/defeat fields; it excludes map, units, cities, resources, yields, diplomacy, orders, notifications, and canonical state. Revocation immediately removes projection authorization and emits a membership resynchronization hint | API-v3 client/session and production owner administration expose invite/revoke; spectators expose read/self-leave without opening a player command bus. Leak, closed-schema, wire, authorization, exact-retry, changed-meaning, membership, outbox, and player-projection-denial tests pass | **Complete for the owner-managed public-summary spectator policy** |
| Game administration | Closed `KickMember` and server-targeted `ForceResign`; idempotent `TransferOwnership`, `CloseGame`, and `ArchiveGame` resource operations | `HeadlessGameEngine` owns canonical AI handoff/turn effects; focused PostgreSQL administration transactions own roles and lifecycle status without fabricating gameplay revisions | Owner and targets derive from authentication, normalized account lookup, membership, canonical current player, and server time; one-owner partial unique index; durable operation IDs reject changed reuse; close serializes with commit locking and gates worker execution plus final commits; archive requires closed | Revisioned commands commit canonical state where required; transfer changes roles; status is projected in metadata/discovery; closed remains readable but immutable; archived remains discoverable without projections; control events force HTTP resync | The production administration popup exposes active-only kick, force-resign, transfer, and close controls, then closed-only archive. Self-resignation has a distinct player/owner server-game control. Retryable commands reuse the open command bus; resource operations retain exact-meaning IDs. No action downloads, mutates, or uploads a save | **Complete for current production administration lifecycle** |
| Chat and social data | Separate authenticated API-v3 friendship, friend-request, player-invitation, and per-game chat resources; never canonical gameplay commands | No worker call: Rust/PostgreSQL own bounded account/channel services, while the revision-zero directory and invitation flow form the v3 lobby | Enabled authenticated accounts; game chat requires current server membership (owner, player, or spectator); caller-stable UUID identity; recipient-only friend acceptance; owner-only game invitation; 1,000-byte message limit, 100-message page limit, 500-message per-game retention, and durable sender/game rate limits | No `GameInfo`, snapshot, command journal, game revision, projection, RNG, or worker mutation. Lobby acceptance alone uses the existing typed `JoinGame` command because server-side civilization assignment is canonical | Shared desktop/Android friends and chat popups plus invitation inbox; owner enters a server-created revision-zero projection immediately, invitees accept into a server-assigned civilization at the current revision, and late joins do not depend on client setup state | **Complete** |
| Camera, selection, overlays, music, local tutorials | None; remain client-local | UI state only | Must not affect canonical state or be uploaded as authority | None | world-screen presentation code | **Not applicable** |

## Cross-cutting requirements for every command

Every implemented row must use the common envelope with protocol version,
game ID, globally unique command ID, expected revision, and diagnostic-only
observed hash. Rust must derive account/civilization/role, enforce request and
entity-ID bounds, return stable authentication/authorization/stale/illegal/
incompatible/rate/internal errors, and commit command, revision, snapshot or
journal reference, actor/turn metadata, and outbox atomically. Duplicate IDs
must be bound to the original actor and exact payload meaning.

Worker handlers must verify the pinned engine and content hashes, load only the
canonical snapshot, call shared Unciv domain logic, use server clock and RNG,
and return structured errors without partial persistence. Client prediction is
optional and disposable; HTTP projection reconciliation is mandatory after
acceptance, rejection, stale conflict, reconnect, or notification.

## Projection gaps blocking command migration

Projection v53 is player-scoped and now covers owned gold, cities, units, spies, pending mod-event choices, legal direct great-person, capital-project, and instant-improvement actions, public research-completion prompts, bounded research-queue actions, known civilizations' public adopted policy branches/ideologies, visible exact movement/swap legality, and city production/purchase/tile economy metadata including closed construction context actions;
visibility-filtered map and foreign units; server-derived movement, combat,
unit-order, production, purchase, population, research, policy, religion,
trade, major-diplomacy and city-state choices; captured-city decisions; diplomatic ballots;
great-person choices; and pending turn blockers. Missing projection contracts
still include fuller terrain/resource information, detailed yields, ideology
flows, private quests, expanded espionage history/results,
notification/history feeds, spectator views, and structured public
events/deltas. Each remaining command milestone must add the narrow legal view
it needs rather than exposing canonical `GameInfo`.

## Completion gate

This matrix is intentionally red. API v3 must not become the default for new
online games until all applicable gameplay rows are complete or an unsupported
mechanic is explicitly disabled server-side and in the online UI. The final
audit must search for direct canonical mutations reachable while
`isOnlineMultiplayer` and prove that each routes through the authoritative bus;
absence from this document is not evidence of completion.
